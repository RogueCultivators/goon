# {{.ProjectName}} API 文档

## 基本信息

- **Base URL**: `http://localhost:8080`
- **API 版本**: v1
- **API 前缀**: `/api/v1`

## 认证

所有需要认证的接口都需要在请求头中携带 JWT Token：

```
Authorization: Bearer <token>
```

## 通用响应格式

### 成功响应

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 错误响应

```json
{
  "code": 400,
  "message": "错误信息",
  "data": null
}
```

## 健康检查

### GET /health

检查服务健康状态

**请求示例：**
```bash
curl http://localhost:8080/health
```

**响应示例：**
```json
{
  "status": "ok",
  "timestamp": 1234567890
}
```

## User API

### 创建用户

**POST** `/api/v1/user`

**请求体：**
```json
{
  "name": "张三",
  "email": "zhangsan@example.com",
  "password": "password123"
}
```

**验证规则：**
- `name`: 必填，2-100 字符
- `email`: 必填，有效的邮箱格式
- `password`: 必填，6-100 字符

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 获取用户列表

**GET** `/api/v1/user`

**查询参数：**
- `page`: 页码（默认：1）
- `page_size`: 每页数量（默认：10，最大：100）
- `keyword`: 搜索关键词（搜索 name 和 email）
- `status`: 状态过滤（active/inactive）

**请求示例：**
```bash
# 基本查询
curl http://localhost:8080/api/v1/user

# 分页查询
curl "http://localhost:8080/api/v1/user?page=1&page_size=20"

# 关键词搜索
curl "http://localhost:8080/api/v1/user?keyword=张三"

# 状态过滤
curl "http://localhost:8080/api/v1/user?status=active"

# 组合查询
curl "http://localhost:8080/api/v1/user?page=1&page_size=10&keyword=张&status=active"
```

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "page": 1,
    "page_size": 10,
    "items": [
      {
        "id": 1,
        "name": "张三",
        "email": "zhangsan@example.com",
        "status": "active",
        "created_at": "2024-01-01T00:00:00Z",
        "updated_at": "2024-01-01T00:00:00Z"
      }
    ]
  }
}
```

### 获取单个用户

**GET** `/api/v1/user/:id`

**路径参数：**
- `id`: 用户 ID

**请求示例：**
```bash
curl http://localhost:8080/api/v1/user/1
```

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "张三",
    "email": "zhangsan@example.com",
    "status": "active",
    "created_at": "2024-01-01T00:00:00Z",
    "updated_at": "2024-01-01T00:00:00Z"
  }
}
```

### 更新用户

**PUT** `/api/v1/user/:id`

**路径参数：**
- `id`: 用户 ID

**请求体：**
```json
{
  "name": "李四",
  "email": "lisi@example.com",
  "status": "inactive"
}
```

**验证规则：**
- `name`: 可选，2-100 字符
- `email`: 可选，有效的邮箱格式
- `status`: 可选，active 或 inactive

**请求示例：**
```bash
curl -X PUT http://localhost:8080/api/v1/user/1 \
  -H "Content-Type: application/json" \
  -d '{
    "name": "李四",
    "status": "inactive"
  }'
```

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 删除用户

**DELETE** `/api/v1/user/:id`

**路径参数：**
- `id`: 用户 ID

**请求示例：**
```bash
curl -X DELETE http://localhost:8080/api/v1/user/1
```

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

**注意：** 这是软删除，用户数据不会真正从数据库中删除。

## 错误码

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 禁止访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

## 测试示例

### 完整的 CRUD 测试流程

```bash
# 1. 创建用户
curl -X POST http://localhost:8080/api/v1/user \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试用户",
    "email": "test@example.com",
    "password": "password123"
  }'

# 2. 获取用户列表
curl http://localhost:8080/api/v1/user

# 3. 获取单个用户（假设 ID 为 1）
curl http://localhost:8080/api/v1/user/1

# 4. 更新用户
curl -X PUT http://localhost:8080/api/v1/user/1 \
  -H "Content-Type: application/json" \
  -d '{
    "name": "更新后的名字",
    "status": "inactive"
  }'

# 5. 删除用户
curl -X DELETE http://localhost:8080/api/v1/user/1
```

## 开发说明

### 添加新的 API 端点

1. 在 `internal/<module>/schema.go` 中定义请求和响应结构
2. 在 `internal/<module>/handler.go` 中实现处理器
3. 在 `internal/<module>/routes.go` 中注册路由
4. 更新本文档

### API 设计规范

- 使用 RESTful 风格
- 使用复数形式的资源名称（如 `/users` 而不是 `/user`）
- 使用 HTTP 方法表示操作（GET/POST/PUT/DELETE）
- 使用统一的响应格式
- 使用合适的 HTTP 状态码
